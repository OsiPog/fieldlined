{
    "scripts": {
        "dev": "npm run wasm-build && vite",
        "build": "vite build",
        "preview": "vite preview",
        "cargo-build": "cd lib && cargo build --target wasm32-unknown-unknown",
        "wasm-build": "wasm-pack build ./lib --target web"
    },
    "dependencies": {
        "vite": "^4.3.9",
        "vite-plugin-top-level-await": "^1.3.1",
        "vite-plugin-wasm": "^3.2.2"
    }
}
